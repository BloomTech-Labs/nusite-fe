export interface OAuthTypes {
   base_url: string;
   secret: string;
   client_id: string;
   scope: string;
   redirect_uri: string;
   state: string;
}

export interface Providers {
   provider: string;
}
